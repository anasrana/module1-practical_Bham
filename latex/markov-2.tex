\documentclass{article}
\include{preview}
\usepackage[pdftex,active,tightpage]{preview}
% \usepackage{amsmath}
\usepackage{tikz}
\usepackage{pgf-pie}
\usetikzlibrary{arrows,automata, calc}
\tikzset{node distance=4.5cm, % Minimum distance between two nodes. Change if necessary.
         every state/.style={ % Sets the properties for each state
           semithick,
           fill=gray!10},
         initial text={},     % No label on start arrow
         double distance=4pt, % Adjust appearance of accept states
         every edge/.style={  % Sets the properties for each transition
         draw,
           ->,>=stealth',     % Makes edges directed with bold arrowheads
           auto,
           semithick}}

           \def\myfrac#1{\expandafter\expandafter\expandafter\expandafter\expandafter%
           \expandafter\expandafter\myfracaux\expandafter\expandafter\expandafter#1}
         \def\myfracaux#1/#2\relax{{\Huge\color{white}$\piestyle\frac{#1}{#2}$}}
         \def\piestyle{\textstyle}
% \usetikzlibrary{matrix}
%% INSERT YOUR OWN CODE HERE
\begin{document}
\begin{preview}
 \begin{tikzpicture}
    \pie[color={yellow!50,blue!70,red!70}, sum=auto, before number=\myfrac, after number=\relax,text=white, font=\bfseries]
{{1/2}/,{1/4}/,{1/4}/}

   \end{tikzpicture}
\end{preview}
\end{document}